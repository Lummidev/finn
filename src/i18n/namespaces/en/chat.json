{
  "clearChat": "Clear Chat",
  "dateGroups": {
    "todayOrYesterday": "{{diff, relativeTime(range: days; numeric: auto)}}",
    "sameWeek": "{{date, dateTime(weekday: long)}}",
    "default": "{{date, dateTime(style: short)}}"
  }
}
